# Configuration pour les tests de charge
# Copier ce fichier en .env et ajuster selon vos besoins

# URL de base pour les tests
# Par défaut : https://api.koprogo.com
BASE_URL=https://api.koprogo.com

# IMPORTANT: Pour des tests de charge précis, assurez-vous que le rate limiting
# est désactivé sur l'API cible (ENABLE_RATE_LIMITING=false)
#
# Si vous utilisez docker-compose.loadtest.yml, le rate limiting est
# automatiquement désactivé via backend/.env.loadtest

# Exemples pour différents environnements :

# Production (défaut)
# BASE_URL=https://api.koprogo.com

# Autre domaine
# BASE_URL=https://api.votredomaine.com

# Développement local avec docker-compose.loadtest.yml
# BASE_URL=http://localhost:8080

# Staging
# BASE_URL=https://staging-api.koprogo.com

# Rust
target/
# Cargo.lock should be committed for applications (not libraries)
# Cargo.lock
*.rs.bk
*.pdb

# Node
node_modules/
dist/
.astro/
.env
.env.local
# package-lock.json should be committed for reproducible builds

# Database
*.db
*.db-shm
*.db-wal

# IDE
.vscode/
.idea/
*.swp
*.swo
*.swn
.DS_Store

# Logs
*.log
logs/

# Docker
.dockerignore

# Test coverage
coverage/
tarpaulin-report.html

# Benchmarks
target/criterion/

# Python
.venv/

# Artefacts du déploiement ecosolva, écrits par `/etc/cron.d/ecosolva-auto-deploy`
# dans l'arbre de travail : verrou, révision déployée, compose et script
# recopiés. Ils appartiennent à la machine, pas au dépôt.
.deploy-ecosolva.lock
.deployed_rev_ecosolva
deploy-ecosolva.sh
docker-compose.ecosolva.yml
docker-compose.ecosolva.yml.bak-*
__pycache__/
*.pyc

# Sphinx docs
docs/_build/

backend/uploads/*

podcasts

# Maury method is intentionally tracked and published under AGPL-3.0.
# It is the canonical entry document a virgin Claude agent reads first.

# Terraform state — managed by remote backend, never committed
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log

# Secrets and keys
**/age.key
**/age.txt
**/.vault_pass
**/.vault_pass.txt
**/.ansible-vault-password
**/*.pem
**/*.key
!**/*.pub.key
**/id_rsa
**/id_ed25519
**/id_ecdsa
**/kubeconfig
**/.kube/config

# .env files (real ones; .env.example is tracked)
#
# Les motifs ci-dessous nomment chaque variante une par une. Une SAUVEGARDE
# n'en portait aucun : `.env.bak-20260906`, écrit par le script de rotation du
# 2026-09-06, contenait `POSTGRES_PASSWORD` en clair, à la racine du dépôt,
# ni suivi ni ignoré. Un seul `git add -A` l'aurait publié.
#
# D'où le glob : ce qui compte est le PRÉFIXE `.env`, pas la liste des suffixes
# qu'on a pensé à écrire.
.env*
!.env.example
!.env.production.example
.env
.env.local
.env.production
.env.staging
.env.integration
.env.loadtest
backend/.env
backend/.env.loadtest
frontend/.env
frontend/.env.local
# Leaked artifact from `docker compose cp` to regenerate api.d.ts —
# the source of truth lives at docs/api/openapi.json
frontend/openapi.json
infrastructure/monosite/local/*/.env
infrastructure/monosite/vps/*/.env

# Claude Code local state
.claude/worktrees/
.claude/settings.local.json
.claude/_drafts/
.claude/scheduled_tasks.lock

# Audit reports / temporary scan outputs
gitleaks-report.json
trivy-report.json
tfsec-report.json
*.sarif

# Office binaries (archive in GitHub Releases instead — see issue #426)
*.docx
*.pdf
!docs/exports/**/*.pdf
!docs/whitepapers/**/*.pdf
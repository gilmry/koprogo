apiVersion: v1
kind: Secret
metadata:
  name: {{ include "koprogo-api.fullname" . }}
  labels:
    {{- include "koprogo-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

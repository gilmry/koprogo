# Terraform Module: Full Kubernetes Cluster on OVH OpenStack
# Future module for multi-node production Kubernetes
# Currently uses the same OVH OpenStack approach as K3s with higher agent counts

# TODO: Implement when multisite K8s is needed
# For now, use ovh-k3s module with higher agent_count

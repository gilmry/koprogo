# Agent activity — 2026-05-18 — WP-B3 triage BDD (#540)

Persona : test-infra / BDD triage. Tier-2 (diagnostic + fix + validation locale) loggé ici ; actions Tier-1 (push, PR, commentaire #540) explicitement autorisées par l'humain en cours de session.

## Contexte

WP-B3 / #540 : ~27 scénarios BDD pré-existants rouges révélés par le harness honnête #524. Recette : reproduire le rouge → comprendre la cause (test/prod/spec) → fix RED-first, jamais « comme ça ».

## Tier-1 préalable (autorisé par l'humain : « merge tout en local sur feature/dev »)

- Merge local des 5 PR (#538 #537 #535 #539 #536) sur `feature/dev` — zéro conflit.
- Branche `story/540-bdd-triage` recréée sur `feature/dev` mergé (substrat : #524 + get_host + C1 Decimal).

## Diagnostic + fix par groupe (Tier-2)

| Groupe | Cause tranchée | Classe | Fix |
|---|---|---|---|
| P1-G1 Resolutions 14/14 | create_resolution exige quorum (Art. 3.87 §5, #310/#323) ; Background ne le validait pas. Bug secondaire : labels legacy `Simple/Qualified` jamais migrés vers l'enum belge | spec obsolète | step Background `the AG is validly constituted (quorum reached)` ; `then_majority_type` + 2 scénarios → `Absolute/TwoThirds` ; + scénario `@security` no-double-vote |
| P1-G2 Board mandat ×5 | `board_member.rs` appliquait la règle **syndic** (Art. 3.89, 1–3 ans) au **conseil de copropriété** (Art. 3.90, ~1 an). DB `mandate_duration_one_year` (330–395) + tests + `legal_compliance.feature` unanimes ; l'entité avait dérivé | **bug prod** | Full Option A : entité 330–395, message + réf Art.3.90, DTO défaut 1095→365, use-cases, 17 tests unitaires 4-cat, 3 steps renew |
| P2 Call-for-Funds / Energy-create / Notices-expiration / Gamification | dates calendaires hardcodées (`2026-03-31`, `2026-04-30`, `2026-04-01`, `2026-04-01/30`) devenues passées ; invariants domaine **corrects** | test (time-bomb) | helper `parse_seed_date` (tokens relatifs `+Nd/-Nd/+Nm/+Ny`, fallback absolu) ×3 binaires + 4 features en tokens relatifs |
| P2 Notices « List author's notices » | seed crée par user_id (tuple `.1`), query liste par owner_id (`.0`) | test (id mismatch) | query → `list_author_notices(user_id)` |
| P2 Energy « Get my uploads » | `when_list_my_uploads` passe `unit_id` à `get_my_uploads(uploaded_by: user_id)` | test (id mismatch) | seed persiste l'uploader ; query `get_my_uploads(uploader_id)` |

Infra (« débruiter le gate », pattern prouvé #535/#539, idempotent, neutre CI) : `get_host()` ajouté à `bdd.rs`, `bdd_operations.rs`, `bdd_community.rs` → les 5 binaires BDD exécutables en local (testcontainers sibling via `host.docker.internal`).

## Reproduction & validation

- Reproduction honnête : CI run `26045738733` (f45d83c) + runs locaux — confirme **exactement** chaque diagnostic (ex. `Resolution creation failed: Quorum has not been validated yet` ; `Error 'Board member mandate cannot exceed 3 years (Art. 3.89 CC)' should contain 'Mandate duration must be approximately 1 year'` ; `violates constraint mandate_duration_one_year`).
- Validation locale (docker compose exec, série, OOM-safe — un seul run à la fois, zéro appel API docker pendant) :
  - `bdd_governance` G1_EXIT=0 — resolutions 15/15
  - `bdd` G2_EXIT=0 — board 23/23 + 13/13
  - `bdd_operations` OPS_EXIT=0 ; `bdd_community` COM_EXIT=0 ; `bdd_financial` Call-for-Funds vert
- Critère #540 atteint : seul rouge restant = **#526** (expenses Zero/Tiny `expenses_amount_check`), explicitement hors-scope, tracé séparément. Zéro rouge non-tracé, zéro régression.

## Incident infra

Docker Desktop a OOM-crashé 2× pendant les runs testcontainers (HTTP 500 / pipe absent). Recovery = restart GUI humain (Tier-1). Mitigation efficace : un seul binaire BDD à la fois + aucun appel API docker concurrent pendant le run (monitoring fichier-log uniquement).

## Tier-1 réalisé (autorisé explicitement)

- Commit groupé `73e4651` (13 fichiers) sur `story/540-bdd-triage`.
- Push + PR draft **#541** → `feature/dev`.

## Suivi requis (tracé sur #540)

- **ADR à signer** : la durée du mandat conseil de copropriété « ~1 an » (330–395 j) est un **choix de modélisation projet** ; Art. 3.90 CC réel laisse la durée à l'AG / au règlement. Décision dirigeante à formaliser (ADR), au-delà du périmètre « débruiter » de WP-B3.
- Note diff : gros diff brut `board_member.rs` = drift rustfmt corrigé par le hook PostToolUse (CRITICAL.md #8) ; logique réelle ~60 lignes (`git diff -w`).

# Agent activity — 2026-07-31 — Audit #433 (umbrella f64→Decimal PCMN) + exécution

**Persona :** audit + exécution (Tier 2 audit/proposition ; Tier 1 fermetures/ADR/nouvelles issues validées en session par @gilmry).

**Contexte :** demande utilisateur « fais l'audit #433 », suite du triage `docs/agent-activity/2026-07-26-triage-stale-restantes.md` §D2, sur le même modèle que l'audit #618 déjà fait.

---

## Méthode

Vérification ligne par ligne de chaque story listée dans le corps de #433 (EXP-003→008, FIN-001, PCT-001, 3 ADRs, 2 hooks CI) contre l'état réel du code (`grep f64/f32`, lecture des migrations SQL, lecture ADR-0008), pas seulement contre les déclarations du WBS.

## Résultat

**7 stories sur 9 réellement faites, vérifiées en code** :

| Story                        | Fichier(s)                                   | Statut code                                    |
| ---------------------------- | -------------------------------------------- | ---------------------------------------------- |
| EXP-003                      | `expense.rs`, `invoice_line_item.rs`         | ✅ 0 `f64` monétaire                           |
| EXP-004                      | `budget.rs`                                  | ✅ 0 `f64` (fait via WP-CL4/H11, Track H #618) |
| EXP-005                      | `charge_distribution.rs`                     | ✅ `Decimal`                                   |
| EXP-006                      | `journal_entry.rs`                           | ✅ `Decimal`                                   |
| EXP-007                      | `etat_date.rs`                               | ✅ `Decimal` (`unit_area` carve-out ADR-0009)  |
| EXP-008                      | `owner_contribution.rs`, `call_for_funds.rs` | ✅ `Decimal`                                   |
| FIN-001                      | `financial_report_use_cases.rs`              | ✅ 0 `f64`                                     |
| SQL-MIGRATION-001 (#438)     | `unit_owners.ownership_percentage`           | ✅ `NUMERIC(6,5)`                              |
| Gouvernance (#521/#525/#534) | `units.quota`, `meetings.*_quotas`           | ✅ `NUMERIC(10,4)`                             |

**1 story jamais touchée** : `payment_reminder.rs` — 100% `f64` (`amount_owed`, `penalty_amount`, `total_amount`, `BELGIAN_PENALTY_RATE`), colonne DB encore `DOUBLE PRECISION`, **absente de la liste fermée des carve-outs ADR-0008**. Confirmé par `bdd_financial.rs` qui garde `last_reminder_penalty: Option<f64>`.

**2 promesses jamais tenues** : hook clippy anti-f64 (aucun `clippy.toml`) et step CI grep anti-f64 (absent de `.github/workflows/`).

**1 ADR encore non ratifiée** : Amendment ADR-0008 (WP-A7, liste fermée des carve-outs) restait `Proposed` malgré le code déjà en prod s'appuyant dessus.

**3 gaps mineurs documentés mais non bloquants** : `quote.rs` (scores `f32`, non-monétaire), `age_request.rs::calculate_progress_percentage` (Decimal interne, f64 à l'affichage seulement), `ag_session.rs` (chemin quorum f64 parallèle — déjà tracé séparément **#661**, hors scope #433).

**Trouvaille annexe** : commentaire SQL `payment_reminders.penalty_amount` disait "8% annual" alors que la constante Rust est `0.045` (4.5%) — divergence signalée à @gilmry, confirmé **4.5% correct**.

## Décisions validées par @gilmry (2026-07-31, message unique numéroté)

1. **Fermer #439, #438, #455, #521, #525, #534** — code vérifié fait, GitHub en retard (même pattern que #603/604/636/339 du triage précédent). ✅ Fait.
2. **Mettre à jour la checklist de #433** pour refléter l'état réel (cocher les 7 stories faites, laisser payment_reminder + hooks CI non cochés, lier les spinoffs). ✅ Fait.
3. **Créer une issue dédiée pour `payment_reminder.rs`** (seul vrai résidu monétaire). ✅ Fait → **#685**.
4. **Taux correct = 4.5%** (pas 8%). ✅ Commentaire SQL `20251107120000_create_payment_reminders.sql` corrigé.
5. **Accepter l'amendement ADR-0008** + **créer un ticket pour les 2 hooks CI**. ✅ ADR-0008 amendment `Proposed` → `Accepted` (pattern identique au commit `f660cfe4` ADR-0010/11/12). ✅ Issue créée → **#686**.

## Fichiers modifiés

- `backend/migrations/20251107120000_create_payment_reminders.sql` — commentaire de colonne 8%→4.5%.
- `docs/adr/0008-numeric-vs-double-precision-postgresql.md` — statut amendement Proposed→Accepted.
- Issue #433 (GitHub) — body réécrit avec checklist à jour.

## Issues GitHub touchées

- Fermées : #439, #438, #455, #521, #525, #534 (Tier 1)
- Créées : #685 (payment_reminder Decimal), #686 (CI anti-f64) (Tier 1 — création doc publique)
- Éditée : #433 (body, Tier 1)

## Ce qui reste ouvert

- #433 elle-même reste ouverte (dépend de #685 + #686).
- #661 (ag_session.rs quorum f64) — hors scope de cet audit, déjà tracée ailleurs.
- Carve-out formel pour `quote.rs`/`age_request.rs` — non fait, jugé non bloquant (pas de montant/seuil légal en jeu), à trancher plus tard si souhaité.

# Agent activity — 2026-05-19 — WBS WP-A7 (ADR-0008 amendement + #339 rotate + #526)

**Persona :** backend/Rust + architecture agent (Tier 2 — implémente le
code + rédige l'ADR en **proposition**. Pas de mutation prod, pas de
force-push, pas de `--no-verify`).

**Branche :** `story/wbs-a7-adr8-rotate` (depuis `origin/feature/dev`,
sans A4/FE1/A5/A6 — séparation confirmée). Choix humain explicite :
« Draft ADR + rotate #339, accept humain » (AskUserQuestion).

**Scope :** WBS Track A, WP-A7 — (a) ADR-0008 finalisé en **amendement
proposé** (accept = humain au merge), (b) #339 rotate clé API implémenté

- 4-cat, (c) #526 politique documentée.

## Réalisé

### (a) ADR-0008 — amendement « Proposed » (Tier-1 accept humain)

`docs/adr/0008-numeric-vs-double-precision-postgresql.md` : section
**Amendment 2026-05-19** ajoutée, **statut `Proposed`** (la décision
d'origine reste `Accepted`, inchangée). Contenu : liste **fermée** des
carve-outs `f64` autorisés (résolution `*_percentage()` affichage seul ;
ratio proxy `vote.rs` — seuil reste Decimal ; gamification `challenge.rs`
non-PCMN ; surfaces m² ; IoT ADR-0009), invariant ratio/%-display
testable, politique #526, note #339. Acceptation = @gilmry au merge de la
PR (mécanisme de validation Tier-1 — pas de merge autonome).

### (b) #339 — rotation de clé API (était `501 Not Implemented`)

`api_key_handlers.rs::rotate_api_key` implémenté : gate rôle
SYNDIC/SUPERADMIN, transaction (désactivation immédiate de l'ancienne clé

- insertion d'une remplaçante héritant name/description/permissions/
  rate_limit/expires_at), isolation cross-org (SELECT scopé
  `organization_id` → **404 sans fuite d'existence**, jamais 403), audit
  `rotated`, secret rendu une seule fois (`ApiKeyCreatedResponse`, 200).
  **`sqlx::query` non-macro** choisi intentionnellement : pas de
  régénération du cache offline `.sqlx` nécessaire (compile sans DB live —
  daemon Docker absent ; pattern déjà présent côté `etat_date` repo).
  4-cat RED-first dans `tests/e2e_api_keys.rs` :

* `@happy` rotate → 200, nouveau secret `kpg_live_`, métadonnées héritées,
  nouvel id ≠ ancien.
* `@security` ancienne clé `is_active=FALSE` post-rotate (anti-rejeu) +
  exactement 1 clé active (la remplaçante) — vérifié en DB.
* `@negative` rôle owner → **403**.
* `@edge` id inconnu/cross-org → **404** (pas de fuite d'existence).

### (c) #526 — politique `expenses.amount` CHECK (> 0)

Documentée dans l'amendement ADR-0008 §C : **conserver `CHECK (amount > 0)`**
(migration `20260502000000`) ; annulations/avoirs = contre-écritures de
journal PCMN, **pas** de relâche schéma. Déjà confirmé vert (#526/WP-B3,
commit `086c953`) — ici on grave la **décision** (was : finding sans ADR).

## Vérification (daemon Docker absent → fallback cargo hôte, CRITICAL.md #12)

- `cargo check --lib --tests` (SQLX_OFFLINE) : **propre, 0 erreur**
  (handler rotate + 4-cat e2e compilent ; `sqlx::query` non-macro =
  aucun cache offline à régénérer).
- `cargo test --lib api_key` : **3/3 verts** (in-module purs).
- `cargo fmt --check` (`api_key_handlers.rs`, `e2e_api_keys.rs`) :
  **propre**.
- e2e rotate 4-cat : **exécutés en CI** (testcontainers — local
  compile-check, même limite infra que WP-B1/A4/A5/A6, tracée).
- Aucun DTO modifié ⇒ zéro drift openapi/api.d.ts (gates verts).

## Tier-1 — en attente humain

- **Acceptation ADR-0008 amendement** : @gilmry merge de la PR WP-A7
  (statut passe `Proposed` → `Accepted` à ce moment, par l'humain).
- Réconciliation : PR Track-A/FE1 empilées sur `feature/dev`
  (#542 A4, #543 FE1, #544 A5, #545 A6, + cette PR A7) — pas de création
  d'issue (gate Tier-1).

## Critère GO (DoD bêta) adressé

- ADR-0008 finalisé (carve-outs explicites + ratio/%-display + gamification)
  — **proposé**, acceptation humaine pendante.
- #526 décidé & documenté.
- #339 rotate implémenté 4-cat ⇒ **aucun `501` ne part en bêta**.

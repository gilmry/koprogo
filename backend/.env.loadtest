# Load Testing Configuration
# This configuration disables rate limiting for accurate performance testing

DATABASE_URL=postgresql://koprogo:koprogo123@postgres:5432/koprogo_db
JWT_SECRET=load-test-secret-key-not-for-production
RUST_LOG=error
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
UPLOAD_DIR=./uploads
CORS_ALLOWED_ORIGINS=*

# CRITICAL: Disable rate limiting for load tests
ENABLE_RATE_LIMITING=false

# Performance tuning
ACTIX_WORKERS=4
DB_POOL_MAX_CONNECTIONS=20
DB_POOL_MIN_CONNECTIONS=5
DB_POOL_ACQUIRE_TIMEOUT=30

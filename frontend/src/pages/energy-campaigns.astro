---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Achats Groupés d'Énergie - KoproGo">
  <div class="container mx-auto px-4 py-8">
    <div class="mb-6">
      <h1 class="text-3xl font-bold text-gray-900">
        ⚡ Achats Groupés d'Énergie
      </h1>
      <p class="text-gray-600 mt-2">
        Négociez collectivement vos contrats d'énergie et économisez jusqu'à
        25% sur vos factures d'électricité et de gaz.
      </p>
      <div
        class="mt-4 p-4 bg-blue-50 border-l-4 border-blue-400 rounded-md text-sm"
      >
        <p class="text-blue-800">
          <strong>🔒 GDPR-compliant:</strong> Toutes vos données sont chiffrées
          avec AES-256-GCM. K-anonymité garantie (minimum 5 participants). Conforme
          CREG belge.
        </p>
      </div>
    </div>

    <div id="campaign-list-container"></div>
    <div id="create-modal-container"></div>
  </div>
</Layout>

<script>
  import { mount } from "svelte";
  import EnergyCampaignList from "../components/energy-campaigns/EnergyCampaignList.svelte";
  import { authStore } from "../stores/auth";
  import { get } from "svelte/store";

  const auth = get(authStore);
  const organizationId = auth.user?.organization_id || "";

  // Mount Campaign List
  const listContainer = document.getElementById("campaign-list-container");
  if (listContainer) {
    mount(EnergyCampaignList, {
      target: listContainer,
      props: {
        organizationId: organizationId,
      },
    });
  }
</script>
